{
  "compilerOptions": {
    "module": "commonJS",
    "target": "es6",
    "jsx": "preserve",
    "baseUrl": ".",
    "paths": {
      // Assuming your modules live in ./src, update this path as necessary
      "~/*": ["./src/*"],
      "~/services/api/*": ["./mocks/api/*"],
      "~/mocks-data/*": ["./mocks/data/*"],
    },
  },
  // Add any build/compiled folders here to stop vscode searching those
  "exclude": [
    "node_modules",
    "dist",
    "build",
    "out",
    ".next",
    "coverage",
  ],
}
